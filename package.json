{
  "name": "lifeos",
  "version": "1.0.0",
  "private": true,
  "description": "LIFE OS — persoonlijke single-file app (index.html). Tests via Playwright.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
